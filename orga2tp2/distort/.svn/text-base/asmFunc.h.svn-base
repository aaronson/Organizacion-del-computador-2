// Antes de compilar el proyecto deben crear y linkear un archivo .asm que implemente al menos sin funcionalidad "asm_rubber_dynamics", la segunda funcion tal como se indica abajo puede implementarla tanto por separado y llamandola desde "asm_rubber_dynamics", o implementandola directamente dentro de "asm_rubber_dynamics"

// Esta es la principal funcion que se debe optimizar, la implementacion esta detallada en su funcion de C "rubber_dynamics".
extern void asm_rubber_dynamics(int mousex, int mousey, int spcount, MASS* massy, SPRING* springy, int grab2);

// La funcionalidad de la funcion updateStates pueden implementarlo tanto como una funcion aparte como dentro de la misma funcion "rubber_dynamics" detallando porque decidieron hacerlo asi en el informe.
extern void asm_updateStates(int mousex, int mousey, int grab2, MASS* massy);
